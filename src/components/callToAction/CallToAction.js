import React from 'react';

const CallToAction = () => {
  return (
    <button onClick={() => console.log('hello')} className="button">
      Online Menu
    </button>
  );
};

export default CallToAction;
